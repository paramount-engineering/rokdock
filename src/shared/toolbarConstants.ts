/** Toolbar height in pixels, shared across the main process and renderer. */
export const TOOLBAR_HEIGHT = 24

/** Default filename format for saved screenshots. Tokens: {YYYY} {MM} {DD} {HH} {mm} {ss}. */
export const DEFAULT_SCREENSHOT_NAMING_FORMAT = 'screenshot-{YYYY}{MM}{DD}-{HH}{mm}{ss}'
