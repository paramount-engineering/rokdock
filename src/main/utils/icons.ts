/**
 * FontAwesome icon serialization helper for main-process template generation.
 *
 * Re-exports faSvg from the shared layer so existing main-process template
 * importers continue to resolve this path without change.
 */

export { faSvg } from '../../shared/icons'
